import Phaser from 'phaser';

export class ExpeditionScene extends Phaser.Scene {
  constructor() {
    super({ key: 'ExpeditionScene' });
  }

  create(): void {
    // Procedural dungeon gameplay will be built here
  }
}
